<Window x:Class="ClaudeAccountSwitcher.RenameAccountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ClaudeAccountSwitcher"
        xmlns:Shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Rename Account" Width="300" Height="182"
        WindowStartupLocation="CenterOwner" WindowStyle="None" ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{StaticResource TitleBarBackgroundBrush}">
    <Shell:WindowChrome.WindowChrome>
        <Shell:WindowChrome CaptionHeight="0" GlassFrameThickness="0" CornerRadius="0" />
    </Shell:WindowChrome.WindowChrome>
    <Border BorderBrush="{StaticResource AppForegroundBrush}" BorderThickness="2">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <local:ThemedTitleBar Grid.Row="0" Title="Rename Account" />

        <StackPanel Grid.Row="1" Margin="18" Background="{StaticResource AppBackgroundBrush}">
            <TextBlock Text="ACCOUNT NAME" FontSize="10" Foreground="{StaticResource AppMutedForegroundBrush}" Margin="0,0,0,6" />
            <TextBox x:Name="NameBox" Style="{StaticResource RoundedTextBoxStyle}" Margin="0,0,0,14" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CancelButton" Content="Cancel" Click="CancelButton_Click"
                        Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0" />
                <Button x:Name="SaveButton" Content="Save" Click="SaveButton_Click"
                        Style="{StaticResource PrimaryButtonStyle}" Padding="12,6" />
            </StackPanel>
        </StackPanel>
    </Grid>
    </Border>
</Window>
