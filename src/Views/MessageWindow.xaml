<Window x:Class="ClaudeAccountSwitcher.MessageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ClaudeAccountSwitcher"
        xmlns:Shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Claude Account Switcher" Width="320" SizeToContent="Height" MinHeight="120"
        WindowStartupLocation="CenterOwner" WindowStyle="None" ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{StaticResource TitleBarBackgroundBrush}">
    <Shell:WindowChrome.WindowChrome>
        <Shell:WindowChrome CaptionHeight="0" GlassFrameThickness="0" CornerRadius="0" />
    </Shell:WindowChrome.WindowChrome>
    <Border BorderBrush="{StaticResource AppForegroundBrush}" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <local:ThemedTitleBar x:Name="TitleBar" Grid.Row="0" />

            <StackPanel Grid.Row="1" Margin="18">
                <TextBlock x:Name="MessageText" FontSize="12" TextWrapping="Wrap"
                           Foreground="{StaticResource AppForegroundBrush}" Margin="0,0,0,16" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="CancelButton" Content="Cancel" Click="CancelButton_Click"
                            Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0" />
                    <Button x:Name="ConfirmButton" Content="OK" Click="ConfirmButton_Click"
                            Style="{StaticResource PrimaryButtonStyle}" Padding="14,6" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
